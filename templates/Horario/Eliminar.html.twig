{% extends 'layout.html.twig' %}
{% block stylesheets %}
{% endblock %}

{% block content %}
    <h1>Eliminar {{ horario.hora }}</h1>

    <div class="alert alert-danger">
        ¿Estás seguro de quitar este horario?
    </div>
    <form method="post">
        <button type="submit" name="confirmar" value="si" class="btnEliminar"><i class="fa fa-trash-alt"></i> Eliminar definitivamente</button>
        <a href="{{ path('horarios_listar') }}" class="btnVolver"><i class="fa fa-arrow-left"></i> Volver al listado</a>
    </form>
{% endblock %}